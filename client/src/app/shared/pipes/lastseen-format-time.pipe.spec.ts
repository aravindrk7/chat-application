import { LastseenFormatTimePipe } from './lastseen-format-time.pipe';

describe('LastseenFormatTimePipe', () => {
  it('create an instance', () => {
    const pipe = new LastseenFormatTimePipe();
    expect(pipe).toBeTruthy();
  });
});
